import { CategoryHub } from "@/components/category-hub";

export default function SalesCategory() {
  return (
    <CategoryHub
      category="Sales & Marketing"
      description="Agents that write personalized outreach, audit SEO content, and flag at-risk accounts before they churn."
    />
  );
}

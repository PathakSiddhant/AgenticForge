import { CategoryHub } from "@/components/category-hub";

export default function MediaCategory() {
  return (
    <CategoryHub
      category="Media & Content"
      description="Turn one idea into scroll-stopping hooks, full scripts, and platform-native posts."
    />
  );
}
